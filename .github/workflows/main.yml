name: Create Release
on:
  push:
    branches:
      - "main"
      - "release/**"
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        
